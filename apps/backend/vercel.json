{
  "buildCommand": "npx medusa db:migrate && cd ../.. && turbo run build --filter=backend && cd apps/backend && node scripts/vercel-post-build.mjs",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "outputDirectory": ".vercel/output",
  "framework": null,
  "regions": [
    "iad1"
  ]
}
